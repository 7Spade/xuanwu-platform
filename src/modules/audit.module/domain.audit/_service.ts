// Audit domain services — logic spanning multiple audit aggregates
// e.g. PolicyEvaluationService   (apply multiple PolicyRules to a single AuditEntry)
//      ComplianceReportService   (aggregate PolicyOutcomes into a ComplianceReport)
