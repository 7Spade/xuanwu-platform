// Audit use cases — policy management and audit log querying
// e.g. CreatePolicyRuleUseCase, UpdatePolicyRuleUseCase, DeletePolicyRuleUseCase
//      EvaluatePolicyUseCase          — checks a pending action against active rules
//      RecordAuditEntryUseCase        — called by domain event subscriber
//      GetAuditTrailUseCase           — query audit entries with filters and pagination
//      ExportAuditReportUseCase       — generate compliance reports
