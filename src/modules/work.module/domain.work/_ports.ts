// Work port interfaces — implemented by infrastructure adapters
// e.g. IWorkItemRepository, IMilestoneRepository, IDependencyRepository
//      IWBSAttachmentPort  — port for attaching work items to WBS Tasks (cross-module via event)
