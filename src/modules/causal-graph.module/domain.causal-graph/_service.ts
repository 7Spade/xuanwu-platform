// Causal-graph domain services — logic spanning multiple causal-graph aggregates
// e.g. CausalPathResolutionService  (traverse CausalNodes and CausalEdges to compute a CausalPath)
//      ImpactScopeService            (determine which downstream CausalNodes are affected by a root cause)
