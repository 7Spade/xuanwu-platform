// Workforce domain services — logic spanning multiple workforce aggregates
// e.g. CapacityAllocationService          (distribute WorkItem load across available member capacity)
//      ScheduleConflictResolutionService  (detect and resolve double-booking or over-allocation conflicts)
