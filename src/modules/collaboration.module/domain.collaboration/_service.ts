// Collaboration domain services — logic spanning multiple collaboration aggregates
// e.g. MentionResolutionService       (resolve @handle mentions to Account references)
//      PresenceAggregationService      (aggregate per-session Presence into a CoEditSession view)
