// Account domain services — logic spanning multiple account aggregates
// e.g. MemberInvitationService   (coordinate Team invite, role assignment, and email notification)
//      AccountTransferService    (transfer org ownership between personal accounts)
//      ProfileSyncService        (sync handle changes with namespace.module via domain events)
