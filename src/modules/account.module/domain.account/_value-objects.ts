// Account value objects — immutable and self-validating
// e.g. AccountId, AccountHandle (globally unique slug), AccountType (personal | organization)
//      DisplayName, AvatarUrl, Bio
//      AccountVisibility (public | private)
//      OwnerId (for organization accounts: the personal AccountId of the owner)
