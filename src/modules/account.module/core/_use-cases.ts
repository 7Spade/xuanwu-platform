// Account use cases — account creation, profile management, team governance, and cross-module projections
// e.g. CreatePersonalAccountUseCase      — triggered after identity registration
//      CreateOrganizationAccountUseCase  — triggered by org provisioning
//      UpdateAccountProfileUseCase
//      ProjectBadgeToAccountUseCase      — handles badge projection from achievement.module
//      GetPublicAccountProfileUseCase
//      CreateTeamUseCase                 — create a team within an org account
//      AddTeamMemberUseCase              — invite and assign role to a member
//      RemoveTeamMemberUseCase           — revoke membership
//      UpdateMemberRoleUseCase           — promote or demote a member's role
