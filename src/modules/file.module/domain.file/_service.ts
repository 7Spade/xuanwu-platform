// File domain services — logic spanning multiple file aggregates
// e.g. DocumentIntelligenceService    (orchestrate DocParse → ObjExtract → Intelligence pipeline)
//      FileVersionConflictService     (detect and resolve concurrent upload version conflicts)
