// Achievement domain services — logic spanning multiple achievement aggregates
// e.g. AchievementEvaluationService  (apply multiple AchievementRules to an incoming event)
//      BadgeGrantService              (orchestrate badge unlock + IAccountBadgeWritePort call)
