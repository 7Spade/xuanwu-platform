// Search domain services — logic spanning multiple search aggregates
// e.g. SearchIndexSyncService      (propagate source-module changes to SearchIndexEntry records)
//      SearchResultRankingService  (apply relevance scoring and access-control filtering to results)
