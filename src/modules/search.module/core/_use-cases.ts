// Search use cases — index management and query execution
// e.g. IndexSourceDocumentUseCase   — triggered by domain events from source modules
//      RemoveIndexEntryUseCase       — triggered by deletion events
//      ExecuteSearchQueryUseCase     — main search query with filters, pagination, ranking
//      SuggestSearchTermsUseCase     — auto-complete / type-ahead
