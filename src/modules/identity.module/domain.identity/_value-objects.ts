// Identity value objects — immutable and self-validating
// e.g. IdentityId, IdentityProvider (Firebase | Google | GitHub | Email)
//      ProviderUid (external UID from the identity provider)
//      AuthClaims (role, permissions, accountId, accountType embedded in JWT)
//      SessionToken, SessionExpiry
