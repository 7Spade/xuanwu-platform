// Identity queries — server-side reads and DTO type definitions
// e.g. getCurrentIdentityQuery, getSessionStatusQuery, getIdentityByProviderQuery
// DTOs: IdentityDTO, SessionStatusDTO, AuthClaimsDTO
