// Namespace use cases — namespace registration and workspace path resolution
// e.g. RegisterOrganizationNamespaceUseCase  — triggered by org creation
//      RegisterPersonalNamespaceUseCase       — triggered by user creating personal workspace
//      ResolveWorkspacePathUseCase            — resolves <slug>/<workspace> to workspace ID
//      BindWorkspaceToNamespaceUseCase        — attaches a workspace under this namespace
