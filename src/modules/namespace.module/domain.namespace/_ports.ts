// Namespace port interfaces — implemented by infrastructure adapters
// e.g. INamespaceRepository  — CRUD for namespace records
//      INamespaceSlugAvailabilityPort  — check slug uniqueness across org + personal namespaces
