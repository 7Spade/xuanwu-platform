// Settlement domain services — logic spanning multiple settlement aggregates
// e.g. SettlementCalculationService   (compute AR/AP balances across multiple settlement records)
//      ReconciliationService           (match incoming payments against outstanding settlement items)
